def kth_permutation(n, k):
    nums = list(range(1, n + 1))
    k -= 1  # convertir a índice 0-based
    result = []
    for i in range(n, 0, -1):
        fact = math.factorial(i - 1)
        index = k // fact
        result.append(nums.pop(index))
        k %= fact
    return result
