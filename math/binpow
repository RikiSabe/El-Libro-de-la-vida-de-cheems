ll binpow(ll a, ll b, ll MOD) {
    a %= MOD;
    ll ans = 1LL;
    while (b > 0) {
        if (b & 1)
            ans = (ans * a) % MOD;
        a = (a * a) % MOD;
        b >>= 1;
    }
    return ans;
}
