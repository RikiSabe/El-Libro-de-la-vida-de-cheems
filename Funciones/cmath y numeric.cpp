#include <cmath>
ceil()  // redondeo hacia arriba
floor() // redondeo hacia atras
round() // redondeo al entero mas cercano
abs()   // valor absoluto entero
fabs()  // valor absoluto flotante
sqrt()  // raiz cuadrada
cbrt()  // raiz cubica
fmod(a,b)   // resto de la division de "a" entre "b"
			// realiza redondeo hacia el entero más cercano
remainder(a,b)  // resto de la division de "a" entre "b"
			    // realiza redondeo hacia cero.
#include <numeric>
__gcd(a,b) // retorna el maximo comun divisor
__lcm(a,b) // retorna el minimo comun multiplo